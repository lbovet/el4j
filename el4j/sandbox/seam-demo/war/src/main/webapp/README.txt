This directory lists different views for entities. 
There are master views (table with n instances) and detail views (a single instance on
the screen). 

The master and detail views with a number before the .xhtml suffix are ignored 
(they are only provided as samples). 