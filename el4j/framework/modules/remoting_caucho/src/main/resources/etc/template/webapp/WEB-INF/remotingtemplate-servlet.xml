<?xml version="1.0" encoding="UTF-8"?>
<!--
    Spring bean configuration file to collect protocols that must be exported in
    webserver.
-->
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd">

    <!-- $Revision$ -->
    <!-- $URL$ -->
    <!-- $Date$ -->
    <!-- $Author$ -->
    
    <import resource="classpath:etc/template/server/web/burlap-server-config.xml"/>
    <import resource="classpath:etc/template/server/web/hessian-server-config.xml"/>
    
    <bean id="beanNameHandlerMapping" 
        class="org.springframework.web.servlet.handler.BeanNameUrlHandlerMapping"/>
</beans>
