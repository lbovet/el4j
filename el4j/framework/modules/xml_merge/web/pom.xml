<?xml version="1.0" encoding="UTF-8"?>
<project
    xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

	<!-- $Version$ -->
    <!-- $URL$ -->
    <!-- $Date$ -->
    <!-- $Author$ -->

    <parent>
        <groupId>ch.elca.el4j.modules</groupId>
        <artifactId>el4j-framework-module-xml_merge</artifactId>
        <version>1.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>module-xml_merge-web</artifactId>
    <version>${version.module-xml_merge}</version>
    <packaging>war</packaging>
	<name>EL4J web module for XmlMerge</name>
    <description>
        Web application module for XmlMerge module of the EL4J framework.
    </description>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <allowOverrideProjectArtifact>false</allowOverrideProjectArtifact>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.cargo</groupId>
                <artifactId>cargo-maven2-plugin</artifactId>
                <configuration>
                    <deployer>
                        <deployables>
                            <deployable>
                                <location>${jee-web.war.location}</location>
                                <properties>
                                    <context>${jee-web.context}</context>
                                </properties>
                            </deployable>
                        </deployables>
                    </deployer>
                </configuration>
            </plugin>
        </plugins>
    </build>
     <dependencies>
        <dependency>
            <groupId>ch.elca.el4j.modules</groupId>
            <artifactId>module-xml_merge-common</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>servlet-api</artifactId>
            <version>[2.5]</version>
        </dependency>
    </dependencies>
    <properties>
    	<!-- 
    		This property overrides the default j2ee-web.context property
    	    defined in the pom.xml file of the external folder.
     	-->
    	<jee-web.context>xmlmerge</jee-web.context>
    </properties>
</project>