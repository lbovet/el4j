<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <!-- Version: $Revision$ -->
    <!-- URL: $URL$ -->
    <!-- Date: $Date$ -->
    <!-- Author: $Author$ -->

    <parent>
        <groupId>ch.elca.el4j.applications</groupId>
        <artifactId>el4j-templates-jsf-demo</artifactId>
        <version>1.2.1-SNAPSHOT</version>
    </parent>

    <artifactId>jsf-demo-war</artifactId>
    <version>${version.application-keyword}</version>
    <packaging>war</packaging>
    <name>EL4J JSF demo war</name>
    <description>WAR part of JSF Demo</description>
    
    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.cargo</groupId>
                <artifactId>cargo-maven2-plugin</artifactId>
                <configuration>
                    <deployer>
                        <deployables>
                            <deployable>
                                <location>${jee-web.war.location}</location>
                                <properties>
                                    <context>${jee-web.context}</context>
                                </properties>
                            </deployable>
                        </deployables>
                    </deployer>
                </configuration>
            </plugin>
            
            
            <!-- EL4J Database plugin to start and fill DB automatically -->
            <plugin>
                <groupId>ch.elca.el4j.maven.plugins</groupId>
                <artifactId>maven-database-plugin</artifactId>
                <executions>
                    <execution>
                        <id>prepare</id>
                        <goals>
                            <goal>prepare</goal>
                        </goals>
                        <phase>test-compile</phase>
                    </execution>
                    <!-- Delete tables and stop Network Server -->
                    <execution>
                        <id>clean up</id>
                        <goals>
                            <goal>silentDrop</goal>
							<goal>stop</goal>
                        </goals>
                        <phase>package</phase>
                    </execution>
                </executions>
            </plugin>
            
            <!-- maven jetty plugin for comfortable development -->
            <plugin>
                <groupId>org.mortbay.jetty</groupId>
                <artifactId>maven-jetty-plugin</artifactId>
                <version>6.1.5-el4j_20080211_2116</version>
                <configuration>
                    <classesDirectory>../jar/target/classes</classesDirectory>
					<!-- exclude the jsf-demo-jar artifact as it is already included via classesDirectory -->
					<excludedArtifacts>
						<excludedArtifact>
							ch.elca.el4j.applications:jsf-demo-jar:jar:${version.application-keyword}
						</excludedArtifact>
					</excludedArtifacts>
                    
                    <scanIntervalSeconds>3</scanIntervalSeconds>
                    <scanTargetPatterns>
                        <scanTargetPattern>
                            <directory>src/main/webapp</directory>
                            <includes>
                                <include>**/*.*</include>
                            </includes>
                            <excludes>
                                <exclude>**/*.svn*</exclude>
                            </excludes>
                        </scanTargetPattern>
                    </scanTargetPatterns>
                    <!-- webdefault.xml is needed to disable file locking for static files-->
                    <webDefaultXml>src/main/resources/webdefault.xml</webDefaultXml>
                    <contextPath>${jee-web.context}</contextPath>
                </configuration>
            </plugin>
        </plugins>
        <testResources>
            <testResource>
                <directory>src/main/webapp</directory>
                <includes>
                    <include>**/*.xml</include>
                </includes>
            </testResource>
        </testResources>
    </build>

    <!-- Project dependencies -->
    <dependencies>
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>5.1</version>
            <scope>test</scope>
        </dependency>
        
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>servlet-api</artifactId>
            <version>2.5</version>
        </dependency>
    
        <dependency>
            <groupId>ch.elca.el4j.applications</groupId>
            <artifactId>jsf-demo-jar</artifactId>
        </dependency>
    </dependencies>
    
</project>
