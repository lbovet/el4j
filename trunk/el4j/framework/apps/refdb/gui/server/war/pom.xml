<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Version: $Revision$ -->
    <!-- URL: $URL$ -->
    <!-- Date: $Date$ -->
    <!-- Author: $Author$ -->

    <parent>
        <groupId>ch.elca.el4j.apps</groupId>
        <artifactId>el4j-framework-apps-refdb-gui-server</artifactId>
        <version>1.1.0-1</version>
    </parent>

    <artifactId>refdb-gui-server-war</artifactId>
    <version>${version.application-refdb}</version>
    <packaging>war</packaging>
    <name>EL4J application refdb gui server - war</name>
    <description>War part of RefDb gui server module of the EL4J framework.</description>
    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.cargo</groupId>
                <artifactId>cargo-maven2-plugin</artifactId>
                <configuration>
                    <deployer>
                        <deployables>
                            <deployable>
                                <location>${jee-web.war.location}</location>
                                <properties>
                                    <context>${jee-web.context}</context>
                                </properties>
                            </deployable>
                        </deployables>
                    </deployer>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <groupId>ch.elca.el4j.apps</groupId>
            <artifactId>refdb-gui-server-jar</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.elca.el4j.modules</groupId>
            <artifactId>module-web-war</artifactId>
            <type>war</type>
        </dependency>
    </dependencies>
    <properties>
        <jee-web.context>refdb-gui-server</jee-web.context>
    </properties>
</project>
