<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://java.sun.com/xml/ns/persistence
		http://java.sun.com/xml/ns/persistence/persistence_1_0.xsd"
	version="1.0">
	
	<!-- 
		Configuration file for Hibernate's entity manager,
		only used to get the full JPA support (incl. @PrePersist,... event listeners) 
		-->
	<persistence-unit name="TestEntityManager">
		<provider>org.hibernate.ejb.HibernatePersistence</provider>
		<properties>
			<property name="hibernate.dialect" value="${databaseDialect}"/>
			<property name="hibernate.connection.driver_class" value="${dataSource.driverClass}" /> 
			<property name="hibernate.connection.url" value="${dataSource.jdbcUrl}" /> 
			<property name="hibernate.connection.username" value="${dataSource.username}" /> 
			<property name="hibernate.connection.password" value="${dataSource.password}" />
		</properties>
		
	</persistence-unit>
</persistence>